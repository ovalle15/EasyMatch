const newNodeName = ({onClick}) => {
    return (
        <form>
            <label>
                <input
                onClick={() => onClick()}
                tu
                >
                </input>
            </label>
        </form>

    )
}
